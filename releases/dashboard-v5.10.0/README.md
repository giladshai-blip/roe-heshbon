# Dashboard V5.10.0

Production dashboard release for Core V5.10.1.

Adds 30-day trough date(s), low-zone duration, and overdraft-interest estimates for the 30-day window and low-zone days.

Install `Dashboard.gs`, then run `installDashboardV5100()`.
