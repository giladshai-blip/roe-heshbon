# Gabi 0.7 — Sub-Agent Promotion Note

**Date:** 2026-09-14

The rebuilt Sub-agent architecture was promoted after:
- `dev` was verified as based on current `main` with no behind commits before development;
- five domain owners were defined at version 0.7.0;
- 20/20 architecture regression scenarios passed;
- Source of Truth, anti-double-counting, approval scope and runtime boundaries were preserved;
- no change was required to `docs/project-instructions.md` because the central instruction already defines Gabi as the only conversational/orchestration layer and Sub-agents as internal components.

Promotion changed only agent architecture/documentation/tests; it did not change financial data or execute financial actions.
