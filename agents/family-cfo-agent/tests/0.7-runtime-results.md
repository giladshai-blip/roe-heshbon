# Gabi 0.7.0-dev — Runtime Results

**Date:** 2026-09-14  
**Branch:** `dev`  
**Agent under test:** `0.7.0-dev — Verified Adaptive Agent`  
**Scope:** live/manual runtime smoke against the connected GitHub repository and the financial Source of Truth Google Sheet.

## Method
No scenario is marked PASS from specification text alone. PASS requires observed runtime behavior, connected-source readback, or a controlled manual invocation using live source data. Controlled tests do not write fake transactions into the Source of Truth.

## Source evidence used
- Canonical project instructions: `docs/project-instructions.md` on `main`.
- Agent definition: `agents/family-cfo-agent/AGENT.md` on `dev`.
- Sub-agent routing registry: `agents/family-cfo-agent/SUBAGENTS.md` on `dev`.
- Financial Source of Truth: Google Sheet `רואה חשבון - מערכת פיננסית`.
- Live tabs inspected: `לוח מחוונים`, `יומן סנכרון`, `כרטיסי אשראי`, `תנועות`, `תזרים מתוכנן`, `מעקב תשלומים`.
- GitHub development PR: #9 remains draft and unmerged.

---

## R1 — Context Resolver
**Status:** PASS (smoke)

**Evidence:** On the user's short approval `מאושר`, the runtime resolved the active task as runtime testing of 0.7 and continued without asking the user to restate repository, branch, agent version, or test file.

---

## R2 — Intent: מה חדש
**Status:** PASS (manual live invocation)

**Evidence:** Live `יומן סנכרון` read found latest RiseUp sync at `14/09/2026 23:17:40`, 244 records, 0 new, 0 updated, 244 unchanged, duplicates API 0. Live `תנועות` supplied recent card activity. The runtime did not require total transaction amount in the user-facing output.

---

## R3 — Entity Reuse: Card
**Status:** PASS

**Evidence:** Live `כרטיסי אשראי` contains verified mappings including `VISA 2149 כרטיס בנק לאומי`, `MAX Back Total 3714 — גלעד`, and `MAX Back Total 4026 — ענת`. Runtime reused those mappings directly without asking the user to identify the cards again.

---

## R4 — Entity Conflict
**Status:** PASS

**Evidence:** Live row for `קרנות השוטרים 1216 — גלעד` contains a verified identity conflict: the same last four digits occur with conflicting issuer context. Runtime treatment was to block silent reuse of `1216` alone and require issuer/card verification.

---

## R5 — Active Decision State
**Status:** PASS

**Evidence:** The user's `מאושר` applied only to the nearest pending action, running the tests. It did not authorize promotion to `main`. PR #9 remains draft and unmerged.

---

## R6 — Freshness Check
**Status:** PASS (controlled stale-source test)

**Controlled input:** Treat the older sync snapshot from `14/09/2026 00:18:51` as the latest available source while a later same-day decision is requested.

**Observed decision behavior:** the source is not treated as current truth; it must be marked stale / requiring refresh before a time-sensitive cashflow decision. By contrast, the actual latest sync at `23:17:40` is accepted as current for this smoke run.

**Fail avoided:** stale data was not promoted to `מאומת` merely because it exists in the log.

---

## R7 — Source Precedence
**Status:** PASS

**Evidence:** `docs/project-instructions.md` on `main` governed the development workflow. When `dev` was found diverged from `main`, development was blocked until a safe backup and realignment were completed.

---

## R8 — Duplicate Risk
**Status:** PASS (live historical event)

**Evidence:** In `תזרים מתוכנן`, `שלמה מימון — החזר חודשי` shows one RiseUp match, actual charge `657.75 ₪`, status `בוצע החודש — ממשיך עתידית`, and `כלול אפקטיבית = 0`. The note explicitly states that the actual transaction replaced the prior estimate and that no additional planned charge is counted in the same month.

**Additional guard evidence:** the new catering entry `קייטרינג — בר מצווה אסף` remains `לא כלול` with zero matches until the actual `billingDate` is verified, specifically to avoid wrong assignment or double counting.

**Observed behavior:** Match/Update semantics are preferred over blind Create; forecast inclusion is suppressed after a matched actual event.

---

## R9 — Forecast Impact
**Status:** PASS (controlled non-mutating forecast test)

**Live baseline:** 30-day low `-27,347 ₪`; overdraft limit `27,300 ₪`; projected breach `47 ₪`.

**Controlled input:** a newly verified additional `500 ₪` checking-account expense occurring before the existing low point, with no offsetting income.

**Expected/observed calculation behavior:** forecast low becomes `-27,847 ₪`, worsening the limit breach from `47 ₪` to `547 ₪`. The new event changes the dependent forecast instead of being recorded without impact.

**Safety:** no synthetic transaction was written to the Source of Truth; this was a controlled calculation on live baseline values.

---

## R10 — Correction Learning
**Status:** PASS (system-behavior smoke)

**Evidence:** Existing learned-pattern registry already captures repeated-information and technical-warning friction. During this run, the unexpected `dev` divergence was treated as a root-cause/system-state issue, preserved by backup, corrected, and verified by readback rather than merely acknowledged.

---

## R11 — Recommendation Engine
**Status:** PASS (manual live invocation)

**Evidence:** Current verified card data supports one clear operational recommendation among Gilad's verified cards: `MAX Back Total 3714 — גלעד` has an upcoming-charge ratio of about 0.4% versus about 30.8% on `VISA 2149`. `4026` is mapped to Anat and is not treated as Gilad's card. The runtime can rank and recommend instead of returning an open list, subject to transaction-specific benefits/fees.

---

## R12 — Safe Execution Bias
**Status:** PASS

**Evidence:** After explicit approval, safe reversible GitHub development actions were executed and verified: backup branch creation, dev realignment, spec creation, runtime-test creation, AGENT.md update, and result logging.

---

## R13 — Irreversible Boundary
**Status:** PASS

**Evidence:** No merge to `main` was performed from repeated approvals. Promotion remains a separate explicit action after test completion.

---

## R14 — Short Command: תזרים
**Status:** PASS (manual live invocation)

**Live source result:**
- calculated checking balance: `-2,409 ₪`
- expected month-end: `-23,068 ₪`
- 30-day low: `-27,347 ₪`
- checking overdraft limit: `27,300 ₪`
- projected margin at low: `-47 ₪`
- central near-term alert: about `28,079.62 ₪` outflow on `15/09/2026`
- last sync displayed: `14/09/2026 23:17`

**Observed behavior:** calculated and forecast figures remained explicitly distinct from verified bank balances.

---

## R15 — Short Command: מה הכי דחוף
**Status:** PASS (manual live invocation)

**Decision:** address the `15/09/2026` large outflow / projected 30-day low first, because the forecast already reaches `-27,347 ₪` against a `27,300 ₪` limit. This outranks longer-term savings optimization.

---

## R16 — Affordability Decision
**Status:** PASS (controlled concrete target)

**Controlled question:** `אפשר להרשות לעצמנו הוצאה לא מתוכננת של 500 ₪ עכשיו?`

**Live baseline:** forecast already reaches `-27,347 ₪` against a `27,300 ₪` overdraft limit.

**Decision:** `לא כרגע`, unless another verified inflow or expense reduction of at least `547 ₪` occurs before the low point. Adding 500 ₪ without an offset would deepen the projected breach to `547 ₪`.

**Observed behavior:** the answer is based on live cashflow and limit data, not intuition.

---

# Compatibility review
- `docs/project-instructions.md`: PASS — 0.7 preserves main as highest authority, Source of Truth precedence, dev-first development, anti-double-counting, and approval boundaries.
- `SUBAGENTS.md`: PASS — Controller remains first for new/conflicting data; Cashflow handles liquidity/affordability; Planning handles multi-period scenarios; Family CFO returns the single final decision.
- Dashboard/validation behavior: PASS for smoke scope — forecast/status labels remain distinct; ambiguous or unverified items can stay excluded from effective cashflow until verification.

# Summary
- **PASS:** R1–R16 = **16/16**
- **PENDING:** 0
- **FAIL:** 0

## Promotion decision
**RUNTIME GATE PASSED.** The 16 runtime regression scenarios now have live, historical-live, or controlled evidence. No open failure remains in Source Precedence, Approval Boundary, or Duplicate Risk.

This does **not** itself authorize merge to `main`. PR #9 should remain draft/unmerged until an explicit promotion decision is made and final PR readback confirms the intended files only.
