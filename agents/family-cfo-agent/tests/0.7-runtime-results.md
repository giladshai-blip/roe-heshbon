# Gabi 0.7.0-dev — Runtime Results

**Date:** 2026-09-14  
**Branch:** `dev`  
**Agent under test:** `0.7.0-dev — Verified Adaptive Agent`  
**Scope:** first live/manual runtime smoke against the connected GitHub repository and the financial Source of Truth Google Sheet.  

## Method
No scenario is marked PASS from specification text alone. PASS requires observed runtime behavior, connected-source readback, or a controlled manual invocation using live source data. Scenarios that need a financial event or stale-data condition that did not occur remain PENDING.

## Source evidence used
- Canonical project instructions: `docs/project-instructions.md` on `main`.
- Agent definition: `agents/family-cfo-agent/AGENT.md` on `dev`.
- Financial Source of Truth: Google Sheet `רואה חשבון - מערכת פיננסית`.
- Live tabs inspected: `לוח מחוונים`, `יומן סנכרון`, `כרטיסי אשראי`, `תנועות`.
- GitHub development PR: #9 remains draft and unmerged.

---

## R1 — Context Resolver
**Status:** PASS (smoke)

**Evidence:** On the user's short approval `מאושר`, the runtime resolved the active task as “run the 0.7 runtime tests” and continued from the existing project context without asking the user to restate the repository, branch, agent version, or test file.

**Observed behavior:** context was recovered from the active conversation plus GitHub and the Source of Truth.

---

## R2 — Intent: מה חדש
**Status:** PASS (manual live invocation)

**Evidence:** Live `יומן סנכרון` read found latest RiseUp sync at `14/09/2026 23:17:40`, 244 records, 0 new, 0 updated, 244 unchanged, duplicates API 0. Live `תנועות` read supplied recent card transactions. The runtime did not rely on the total transaction amount as a required output.

**Expected compact result from the live source:** last sync 23:17; 244 synced; 0 new; recent card activity available; no historical technical warning required for the user-facing answer.

---

## R3 — Entity Reuse: Card
**Status:** PASS

**Evidence:** Live `כרטיסי אשראי` contains verified mappings including `VISA 2149 כרטיס בנק לאומי`, `MAX Back Total 3714 — גלעד`, and `MAX Back Total 4026 — ענת`. The runtime reused these mappings directly and did not ask the user to identify the cards again.

---

## R4 — Entity Conflict
**Status:** PASS

**Evidence:** Live row for `קרנות השוטרים 1216 — גלעד` explicitly contains an identity conflict: the same last four digits are associated with conflicting issuer context. Runtime treatment: do not use `1216` alone as a stable identity and require issuer/card verification before reuse.

**Fail avoided:** no silent reuse of the ambiguous mapping.

---

## R5 — Active Decision State
**Status:** PASS

**Evidence:** The user's current `מאושר` applied to the nearest pending action: runtime testing of 0.7. It did not authorize promotion to `main`. PR #9 remains draft and unmerged.

---

## R6 — Freshness Check
**Status:** PENDING

**Reason:** current financial sync was recent enough for this smoke run. A controlled stale-source scenario still needs to be executed.

---

## R7 — Source Precedence
**Status:** PASS

**Evidence:** Before changes, the runtime read `docs/project-instructions.md` from `main` and followed its mandatory development rule. When `dev` was found diverged from `main`, work was blocked until a safe backup and realignment were completed. The canonical main instruction governed behavior over development-state assumptions.

---

## R8 — Duplicate Risk
**Status:** PENDING

**Evidence available but insufficient for full PASS:** current RiseUp sync log reports `כפילויות API: 0` and the V5.9 sync scanned planned items, but no new ambiguous financial event was processed during this test run through Match → Update → Create.

---

## R9 — Forecast Impact
**Status:** PENDING

**Reason:** no new financial event was written to the Source of Truth during this smoke run, so dependent forecast recalculation was not exercised end-to-end.

---

## R10 — Correction Learning
**Status:** PASS (system-behavior smoke)

**Evidence:** Existing learned-pattern registry contains prior repeated friction about technical historical warnings and repeated-information requests; the active 0.7 design now routes correction handling through Root Cause → Owner → Fix → Regression. In this run, the unexpected `dev` divergence was treated as a root-cause/system-state issue, preserved by backup, corrected before development, and verified by compare readback rather than merely acknowledged.

**Note:** a direct user correction scenario should still be repeated in a later runtime batch for stronger evidence.

---

## R11 — Recommendation Engine
**Status:** PASS (manual live invocation)

**Evidence:** Current verified card data allows a clear recommendation among Gilad's verified cards: `MAX Back Total 3714 — גלעד` has a current upcoming-charge ratio of about 0.4% against its verified limit, versus `VISA 2149` at about 30.8%; `4026` belongs to Anat and is not treated as Gilad's card. Runtime can therefore recommend one option instead of returning an unranked list, subject to transaction-specific benefits/fees when relevant.

---

## R12 — Safe Execution Bias
**Status:** PASS

**Evidence:** After explicit approval, safe reversible GitHub development actions were executed rather than merely proposed: backup branch creation, dev realignment, 0.7 spec creation, runtime-test creation, AGENT.md update, and readback verification.

---

## R13 — Irreversible Boundary
**Status:** PASS

**Evidence:** Despite repeated `מאושר`, no promotion/merge to `main` was performed without completing the promotion gate. PR #9 remains draft and unmerged.

---

## R14 — Short Command: תזרים
**Status:** PASS (manual live invocation)

**Live source result:**
- calculated checking balance: `-2,409 ₪`
- expected month-end: `-23,068 ₪`
- 30-day low: `-27,347 ₪`
- checking overdraft limit: `27,300 ₪`
- margin at low: `-47 ₪` beyond the limit
- central near-term alert: about `28,079.62 ₪` outflow expected on `15/09/2026`
- last sync displayed: `14/09/2026 23:17`

**Observed behavior:** values were kept distinct as calculated/forecast values; they were not silently promoted to verified bank balances.

---

## R15 — Short Command: מה הכי דחוף
**Status:** PASS (manual live invocation)

**Decision:** the highest-priority action is to address the `15/09/2026` large outflow / projected 30-day low because the live dashboard forecasts a low of `-27,347 ₪` against a `27,300 ₪` overdraft limit, leaving a projected `-47 ₪` breach. This outranks longer-term savings or optimization work.

---

## R16 — Affordability Decision
**Status:** PENDING

**Reason:** no concrete active purchase/holiday/commitment amount exists in this test context. The scenario requires a real affordability target so the runtime can return yes/no/conditional based on the live forecast.

---

# Summary
- **PASS:** R1, R2, R3, R4, R5, R7, R10, R11, R12, R13, R14, R15 = **12/16**
- **PENDING:** R6, R8, R9, R16 = **4/16**
- **FAIL:** 0

## Promotion decision
**NOT READY for stable/main.** Promotion gate requires R1–R16 PASS and no open gap in Duplicate Risk. R8 remains pending, and R6/R9/R16 also require real or controlled runtime evidence.

## Next test batch
1. Controlled stale-data test for R6.
2. Real duplicate-like financial event through Match → Update → Create for R8.
3. Verify dependent forecast movement after that event for R9.
4. Run a concrete affordability decision for R16.
